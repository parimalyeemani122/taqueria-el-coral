const CONFIG = {
  webhookUrl: '', // Set to your order webhook endpoint URL
  stripePublishableKey: '', // Set to your Stripe publishable key for Stripe Checkout
  chatbotUrl: '', // Leave empty to use same origin (Vercel); set to full URL for external chatbot server
};
